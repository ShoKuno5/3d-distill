# Bulk data — never track
datasets/
envs/
results/
archive/

# Model repos (have their own .git)
models/

# Python / OS artifacts
__pycache__/
*.pyc
*.egg-info/
*.so

# Editor / IDE
.vscode/
.idea/
*.swp

# Claude Code working files
.claude/

# Legacy archive backup (same content as archive/ but with dot prefix)
.archive/

# Unrelated tarball (accidentally dropped here)
xkbcommon-1.5.1.tar.gz

# External evaluation models (ULIP, Uni3D) — large checkpoints
pipeline/src/evaluation/external/

# Editor / process backup files
*.bak_*
*.bak.*
